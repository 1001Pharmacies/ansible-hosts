file:
  /etc/bashrc.local:
    exists: true
    mode: "0644"
    owner: root
    group: root
    filetype: file
    contains:
      - /^function git_branch/
      - /^function process_count/
      - /^function load_average/

